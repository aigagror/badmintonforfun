import * as React from "react";
import * as ReactDOM from "react-dom";

import { Queue } from "./components/Queue";

ReactDOM.render(
    <Queue />,
    document.querySelector("queue-view")
);
